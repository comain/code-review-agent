# C/C++ Review Rule Overlay

- Check changed memory ownership, bounds checks, null handling, and lifetime assumptions.
- Check integer conversion, signedness, overflow, and buffer-size calculations.
- Check error paths for leaked resources and inconsistent cleanup.

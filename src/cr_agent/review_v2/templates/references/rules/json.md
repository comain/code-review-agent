# JSON Review Rule Overlay

- Check schema/config changes for missing required fields, renamed keys, enum/value compatibility, and unsafe defaults.
- Check generated or package metadata changes only when they affect runtime behavior, build behavior, or published artifacts.
- Check numeric and boolean settings for unit drift and operationally dangerous values.

# Package Manifest Review Rule Overlay

- Check dependency changes for runtime bundle impact, security-sensitive packages, and major-version compatibility.
- Check script changes for CI behavior, test skipping, environment assumptions, and unsafe shell expansion.
- Check package manager metadata for lockfile consistency and publish/runtime entrypoint compatibility.

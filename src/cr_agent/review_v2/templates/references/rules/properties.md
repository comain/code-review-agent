# Properties Review Rule Overlay

- Check key renames, removed keys, and default changes for backward compatibility across environments.
- Check timeout, retry, batch-size, thread-pool, and endpoint changes for safe bounds.
- Check secret-like values are not introduced into committed config.
